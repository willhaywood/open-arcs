/// <reference types="vite/client" />
//
// Types for `import.meta.env`, which `assets.ts` reads to honour the deployment's base path.
// Vite's convention is this reference file rather than a `types` entry in tsconfig, so the app's
// own tsconfig stays a plain DOM project.
